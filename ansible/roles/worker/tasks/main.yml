- name: Add Worker to manager node
  docker_swarm:
    state: join
    advertise_addr: worker
    join_token: "{{ hostvars['34.89.59.203']['swarm_info']['swarm_facts']['JoinTokens']['Worker'] }}"
    remote_addrs: [ 'swarm-master' ]